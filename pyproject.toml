[project]
name = "motherduck-marketstack"
version = "0.1.0"
description = "AAPL EOD data processed on MotherDuck via Flights and visualized via a Dive."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.0.0",
]

[tool.uv]
package = false
