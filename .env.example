# Copy to .env and fill in a token, then `make deploy-dives` / `make deploy-flights`.
# Use a personal token for trying things out; use a prod_service_account
# token for real deploys, so pushed Dives/Flights are owned by the service
# account rather than an individual.
MOTHERDUCK_TOKEN=
